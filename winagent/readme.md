### File: bucketname

```shell
bucketname
```

`gs://{bucketname}` oneline without carriage return

### File: conf

```shell
project_id
job_name
```

### GCP Service Account

named `windev.json` in the current executing directory
